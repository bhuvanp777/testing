name: Weekly Workflow

on:
  workflow_dispatch:
    

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Run Python script
        run: |
          # Add your Python script execution command here
          python sample.py
          touch some.txt
          echo "hello" > some.txt

      - name: Commit and push changes
        env:
          PAT_TOKEN: "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"
        run: |
          git config --global user.name "bhuvanp777"
          git config --global user.email "bhuvan.p@amberstudent.com"

          git add .
          git commit -m "Update the repository"
          git remote set-url origin "https://github.com/bhuvanp777/testing.git"
          git push origin HEAD
